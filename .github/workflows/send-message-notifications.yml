name: Send Message Notifications

on:
  schedule:
    - cron: '*/5 * * * *' # every 5 minutes
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Call send-message-notifications
        # Uses the anon key (already public — committed in utils/supabase/info.tsx —
        # not a secret). All functions in this repo deploy with --no-verify-jwt, so
        # this only needs to be *a* bearer token, not a privileged one.
        run: |
          curl -sf -X POST \
            "https://snajrdvcwsbjqefaftsj.supabase.co/functions/v1/send-message-notifications" \
            -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InNuYWpyZHZjd3NianFlZmFmdHNqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzUxNzE5NTgsImV4cCI6MjA5MDc0Nzk1OH0.n2oLgMfdfzmLw8KvsBrJbsFvgQ7qOgPL7tVlD-lqDiY" \
            -H "Content-Type: application/json"
